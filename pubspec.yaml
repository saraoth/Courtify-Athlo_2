name: courtify_athlo
description: A high-fidelity smart sports club platform with AI performance insights for Courtify - Athlo.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.5
  google_sign_in: ^6.1.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  fl_chart: ^0.66.0
  intl: ^0.19.0
  animate_do: ^3.1.2
  local_auth: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
